[package]
name = "xcap-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
xcap = "0.8.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
strip = true
lto = true
codegen-units = 1
panic = "abort"
opt-level = "z"
